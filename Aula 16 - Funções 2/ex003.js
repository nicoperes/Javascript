let v = function(x) { // é possível associar uma função para dentro da variável, conforme esta linha
    return x * 2 // multiplicar o parâmetro por 2
}

console.log(v(2)) // chamada definindo o parâmetro da variável para 2, resultado será 4